[project]
name = "agent-system"
version = "0.1.0"
description = "Sunstead agent suite — agent-runner container + shared contracts"
requires-python = ">=3.10"
dependencies = ["shared", "agent-runner"]

[tool.uv.workspace]
members = ["shared", "agent-runner"]

[tool.uv.sources]
shared = { workspace = true }
agent-runner = { workspace = true }
